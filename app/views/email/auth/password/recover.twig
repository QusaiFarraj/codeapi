{% extends 'email/templates/default.twig' %}

{% block content %}
    <p>You requseted a password change.</p>

    <p>Click this link to reset your password: {{ baseUrl }}{{ path_for('password.reset') }}?email={{ user.email }}&identifier={{ identifier|url_encode }}</p>
{% endblock %}